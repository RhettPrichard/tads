langcode: en
status: true
dependencies: 
  enforced:
    module:
      - tads_preqs
  config:
    - user.role.exam_publisher
  module:
    - user
id: user_add_role_action.exam_publisher
label: 'Add the Exam Publisher role to the selected users'
type: user
plugin: user_add_role_action
configuration:
  rid: exam_publisher
