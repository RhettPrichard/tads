langcode: en
status: true
dependencies: 
  enforced:
    module:
      - tads_preqs
  config:
    - user.role.teacher
  module:
    - user
id: user_add_role_action.teacher
label: 'Add the Teacher role to the selected users'
type: user
plugin: user_add_role_action
configuration:
  rid: teacher
