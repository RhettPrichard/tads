langcode: en
status: true
dependencies: 
  enforced:
    module:
      - tads_preqs
  config:
    - user.role.exam_publisher
  module:
    - user
id: user_remove_role_action.exam_publisher
label: 'Remove the Exam Publisher role from the selected users'
type: user
plugin: user_remove_role_action
configuration:
  rid: exam_publisher
