langcode: en
status: true
dependencies:
  enforced:
    module:
      - tads_preqs
name: 'TADS Exam Types'
vid: tads_exam_types
description: 'All tests that qualify a person to work on power plants, would be tagged with "Power Plants"'
hierarchy: 0
weight: 0
